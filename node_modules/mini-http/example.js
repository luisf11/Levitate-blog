const miniHttp = require('./index.js');  // require('mini-http')

let options = {
  port: 6000,                                 // defualts: 3000
  root: __dirname,                            // defaults: the dir you are running the process in
  startingMsg: "server started at port %s ",  // defaults: server started at http://localhost:{ PORT }
  slash: 'index.html',                        // defaults: will show dir available files/dir
  log: true                                   // defaults: false, to log server config
}

miniHttp.serve(options);
